<%- include('../partials/head'); %>
</head>
<body>
<%- include('../partials/navigation'); %>
<div class="border w-full md:flex md:flex-col">
  <h1 class="text-6xl font-bold text-blue-500">Product Details</h1>
  <div class="w-1/2 m-10">
    <div class="rounded overflow-hidden shadow-lg">
      <img class="" src="<%= product.imageUrl %>" alt="<%= product.name %>">
      <div class="px-6 py-2">
        <div class="font-bold text-xl mb-2"><%= product.name %></div>
        <p class="text-gray-700 text-base">
          ₹<%= product.price %>
        </p>
      </div>
      <div class="px-6 py-2">
        <span class="inline-block bg-gray-200 rounded-full px-3 py-1 text-sm font-semibold text-gray-700 mr-2"><%= product.description %></span>
      </div>
    </div>
  </div>
</div>
<%- include('../partials/footer'); %>
<%- include('../partials/end'); %>