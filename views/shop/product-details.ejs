<%- include('../partials/head'); %>
</head>
<body>
<%- include('../partials/navigation'); %>
<%- include('../partials/sliding-navigation'); %>
<%- include('../partials/bottom-nav'); %>
<div class="main flex border justify-center mb-16">
  <%- include('../partials/sidemenu'); %>
  <div class="md:w-5/6 w-full">
  <div class="text-xl font-bold text-gray-700"><%= pageTitle %></div>
  <div class="">
    <div class="flex flex-col rounded overflow-hidden shadow-lg">
      <img class="" src="/<%= product.imageUrl %>" alt="<%= product.name %>" width="200" height="150">
      <div class="px-6 py-2">
        <div class="font-bold text-xl mb-2"><%= product.name %></div>
        <p class="text-gray-700 text-base">
          ₹<%= product.price %>
        </p>
      </div>
      <div class="px-6 py-2">
        <span class="inline-block bg-gray-200 rounded-full px-3 py-1 text-sm font-semibold text-gray-700 mr-2"><%= product.description %></span>
      </div>
      <div class="px-6 py-2">
        <form action="/cart" method="POST">
          <button class="bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded" type="submit">
            <i class="fas fa-cart-plus"></i> Add to cart
          </button>
          <input type="hidden" name="productId" value="<%= product.id %>">
          <input type="hidden" name="_csrf" value="<%= csrfToken %>">
        </form>
      </div>
    </div>
  </div>
  </div>
</div>
  
</div>
<%- include('../partials/end'); %>